export class Category {
    id: number;
    title: string;
    logo: string;

    constructor(id: number, title: string, logo: string) {
        this.id = id;
        this.title = title;
        this.logo = logo;
    }
}